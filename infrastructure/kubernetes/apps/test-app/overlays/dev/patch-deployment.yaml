apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-app
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name:  nginx
        image: nginx:latest
        ports:
        - containerPort: 80
